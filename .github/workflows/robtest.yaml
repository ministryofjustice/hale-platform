name: Rob Test Deployment

on:
  workflow_dispatch:
  push:
    branches:
      - 'robdev'
      - 'robdemo'


jobs:

  job1:
    runs-on: ubuntu-latest
    # Map a step output to a job output
    outputs:
      deployenv: ${{ steps.step1.outputs.test }}
    steps:
      - id: step1
        run: echo "test=RobDev" >> "$GITHUB_OUTPUT"

  testjob:
    name: Test Job
    runs-on: ubuntu-latest
    needs: job1
    environment: ${{needs.job1.outputs.deployenv}}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Authenticate to the cluster
        run: |
          echo "${{ vars.EXAMPLEVAR1 }}" 
          echo "${{ vars.EXAMPLEVAR2 }}" 