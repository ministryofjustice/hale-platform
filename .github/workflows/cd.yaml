name: Continuous Deployment

on:
  workflow_dispatch:
  push:
    branches:
      - 'demo'
      - 'dev'
      - 'main'

jobs:
  buildDemo:
    name: Build Demo and push to ECR
    if: github.event.ref == 'refs/heads/demo'
    runs-on: ubuntu-latest
    environment: Demonstration
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build NGINX image
        run: |
          docker build --pull --no-cache --tag hale-platform_nginx --file nginx.dockerfile .
      - name: Push NGINX image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_DEMO }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_DEMO }}
          region: eu-west-2
          local-image: hale-platform_nginx
          image: ${{ secrets.ECR_NAME_DEMO }}:hale-platform_nginx-${{ github.sha }}
      - name: Run Composer
        run: |
          composer config http-basic.composer.wp.dsd.io ${{ secrets.COMPOSER_USER }} ${{ secrets.COMPOSER_PASS }}
          composer install --prefer-dist --no-progress
      - name: Run NPM
        run: |
          npm install --prefix ./wordpress/wp-content/themes/hale
          npm run production --if-present --prefix ./wordpress/wp-content/themes/hale
      - name: Build WordPress image
        run: |
          docker build --pull --no-cache --tag hale-platform_wordpress --file wp.dockerfile .
      - name: Push WordPress image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_DEMO }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_DEMO }}
          region: eu-west-2
          local-image: hale-platform_wordpress:latest
          image: ${{ secrets.ECR_NAME_DEMO }}:hale-platform_wordpress-${{ github.sha }}

  deployDemo:
    name: Deploy to Demonstration
    if: github.event.ref == 'refs/heads/demo'
    needs: buildDemo
    runs-on: ubuntu-latest
    environment: Demonstration
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Authenticate to the cluster
        env:
          KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER_DEMO }}
        run: |
          echo "${{ secrets.KUBE_CERT_DEMO }}" > ca.crt
          kubectl config set-cluster ${KUBE_CLUSTER} --certificate-authority=./ca.crt --server=https://${KUBE_CLUSTER}
          kubectl config set-credentials deploy-user --token=${{ secrets.KUBE_TOKEN_DEMO }}
          kubectl config set-context ${KUBE_CLUSTER} --cluster=${KUBE_CLUSTER} --user=deploy-user --namespace=${{ secrets.KUBE_NAMESPACE_DEMO }}
          kubectl config use-context ${KUBE_CLUSTER}
      - name: Apply Helm chart
        run: |
          helm upgrade wordpress helm_deploy/wordpress \
          --install \
          --namespace ${{ secrets.KUBE_NAMESPACE_DEMO }} \
          --values helm_deploy/wordpress/values.yaml \
          --set secrets.wpdbuser=${{ secrets.WORDPRESS_DB_USER }} \
          --set secrets.wpdbhost=${{ secrets.WORDPRESS_DB_HOST }} \
          --set secrets.wpdbname=${{ secrets.WORDPRESS_DB_NAME }} \
          --set secrets.wpdbpassword=${{ secrets.WORDPRESS_DB_PASSWORD }} \
          --set secrets.wpadminemail=${{ secrets.WORDPRESS_ADMIN_EMAIL }} \
          --set secrets.wpadminuser=${{ secrets.WORDPRESS_ADMIN_USER }} \
          --set secrets.wpadminpassword=${{ secrets.WORDPRESS_ADMIN_PASSWORD }} \
          --set secrets.wpauthkey=${{ secrets.WORDPRESS_AUTH_KEY_FILE }} \
          --set secrets.wpauthsalt=${{ secrets.WORDPRESS_AUTH_SALT_FILE }} \
          --set secrets.wploggedinkey=${{ secrets.WORDPRESS_LOGGED_IN_KEY_FILE }} \
          --set secrets.wploggedinsalt=${{ secrets.WORDPRESS_LOGGED_IN_SALT_FILE }} \
          --set secrets.wpnoncekey=${{ secrets.WORDPRESS_NONCE_KEY_FILE }} \
          --set secrets.wpnoncesalt=${{ secrets.WORDPRESS_NONCE_SALT_FILE }} \
          --set secrets.wpsecureauthkey=${{ secrets.WORDPRESS_SECURE_AUTH_KEY_FILE }} \
          --set secrets.wpsecureauthsalt=${{ secrets.WORDPRESS_SECURE_AUTH_SALT_FILE }} \
          --set configmap.servername=${{ secrets.KUBE_NAMESPACE_DEMO }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set configmap.wpenvtype=${{ secrets.WP_ENV_TYPE }} \
          --set ingress.metadata.annotations.setidentifier=hale-platform-ingress-${{ secrets.KUBE_NAMESPACE_DEMO }}-green \
          --set domain=${{ secrets.KUBE_NAMESPACE_DEMO }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set nginx.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_DEMO }}:hale-platform_nginx-${{ github.sha }} \
          --set wp.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_DEMO }}:hale-platform_wordpress-${{ github.sha }}

  ecrCleanDemo:
    name: ECR clean - Demo
    if: github.event.ref == 'refs/heads/demo'
    runs-on: ubuntu-latest
    needs: [buildDemo, deployDemo]
    steps:
    - name: Run ECR clean-up script
      uses: ministryofjustice/ecr-images-cleaner-action@v1.0.1
      with:
        aws-access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_DEMO }}
        aws-secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_DEMO }}
        kube-cert: ${{ secrets.KUBE_CERT_DEMO }}
        kube-token: ${{ secrets.KUBE_TOKEN_DEMO }}
        kube-cluster: ${{ secrets.KUBE_CLUSTER_DEMO }}
        kube-namespace: ${{ secrets.KUBE_NAMESPACE_DEMO }}
        ecr-repo-name: ${{ secrets.ECR_NAME_DEMO }}
        days-to-keep-old-images: 10
        max-old-images-to-keep: 24

  buildDev:
    name: Build Dev and push to ECR
    if: github.event.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    environment: Development
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build NGINX image
        run: |
          docker build --pull --no-cache --tag hale-platform_nginx --file nginx.dockerfile .
      - name: Push NGINX image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_DEV }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_DEV }}
          region: eu-west-2
          local-image: hale-platform_nginx
          image: ${{ secrets.ECR_NAME_DEV }}:hale-platform_nginx-${{ github.sha }}
      - name: Run Composer
        run: |
          composer config http-basic.composer.wp.dsd.io ${{ secrets.COMPOSER_USER }} ${{ secrets.COMPOSER_PASS }}
          composer install --prefer-dist --no-progress
      - name: Run NPM
        run: |
          npm install --prefix ./wordpress/wp-content/themes/hale
          npm run production --if-present --prefix ./wordpress/wp-content/themes/hale
      - name: Build WordPress image
        run: |
          docker build --pull --no-cache --tag hale-platform_wordpress --file wp.dockerfile .
      - name: Push WordPress image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_DEV }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_DEV }}
          region: eu-west-2
          local-image: hale-platform_wordpress:latest
          image: ${{ secrets.ECR_NAME_DEV }}:hale-platform_wordpress-${{ github.sha }}

  deployDev:
    name: Deploy to Development
    if: github.event.ref == 'refs/heads/dev'
    needs: buildDev
    runs-on: ubuntu-latest
    environment: Development
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Authenticate to the cluster
        env:
          KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER_DEV }}
        run: |
          echo "${{ secrets.KUBE_CERT_DEV }}" > ca.crt
          kubectl config set-cluster ${KUBE_CLUSTER} --certificate-authority=./ca.crt --server=https://${KUBE_CLUSTER}
          kubectl config set-credentials deploy-user --token=${{ secrets.KUBE_TOKEN_DEV }}
          kubectl config set-context ${KUBE_CLUSTER} --cluster=${KUBE_CLUSTER} --user=deploy-user --namespace=${{ secrets.KUBE_NAMESPACE_DEV }}
          kubectl config use-context ${KUBE_CLUSTER}
      - name: Apply Helm chart
        run: |
          helm upgrade wordpress helm_deploy/wordpress \
          --install \
          --namespace ${{ secrets.KUBE_NAMESPACE_DEV }} \
          --values helm_deploy/wordpress/values.yaml \
          --set secrets.wpdbuser=${{ secrets.WORDPRESS_DB_USER }} \
          --set secrets.wpdbhost=${{ secrets.WORDPRESS_DB_HOST }} \
          --set secrets.wpdbname=${{ secrets.WORDPRESS_DB_NAME }} \
          --set secrets.wpdbpassword=${{ secrets.WORDPRESS_DB_PASSWORD }} \
          --set secrets.wpadminemail=${{ secrets.WORDPRESS_ADMIN_EMAIL }} \
          --set secrets.wpadminuser=${{ secrets.WORDPRESS_ADMIN_USER }} \
          --set secrets.wpadminpassword=${{ secrets.WORDPRESS_ADMIN_PASSWORD }} \
          --set secrets.wpauthkey=${{ secrets.WORDPRESS_AUTH_KEY_FILE }} \
          --set secrets.wpauthsalt=${{ secrets.WORDPRESS_AUTH_SALT_FILE }} \
          --set secrets.wploggedinkey=${{ secrets.WORDPRESS_LOGGED_IN_KEY_FILE }} \
          --set secrets.wploggedinsalt=${{ secrets.WORDPRESS_LOGGED_IN_SALT_FILE }} \
          --set secrets.wpnoncekey=${{ secrets.WORDPRESS_NONCE_KEY_FILE }} \
          --set secrets.wpnoncesalt=${{ secrets.WORDPRESS_NONCE_SALT_FILE }} \
          --set secrets.wpsecureauthkey=${{ secrets.WORDPRESS_SECURE_AUTH_KEY_FILE }} \
          --set secrets.wpsecureauthsalt=${{ secrets.WORDPRESS_SECURE_AUTH_SALT_FILE }} \
          --set secrets.s3uploadsbucket=${{ secrets.S3_UPLOADS_BUCKET }} \
          --set secrets.s3uploadsregion=${{ secrets.S3_UPLOADS_REGION }} \
          --set secrets.s3uploadskey=${{ secrets.S3_UPLOADS_KEY }} \
          --set secrets.s3uploadssecret=${{ secrets.S3_UPLOADS_SECRET }} \
          --set configmap.servername=${{ secrets.KUBE_NAMESPACE_DEV }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set configmap.wpenvtype=${{ secrets.WP_ENV_TYPE }} \
          --set ingress.metadata.annotations.setidentifier=hale-platform-ingress-${{ secrets.KUBE_NAMESPACE_DEV }}-green \
          --set domain=${{ secrets.KUBE_NAMESPACE_DEV }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set nginx.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_DEV }}:hale-platform_nginx-${{ github.sha }} \
          --set wp.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_DEV }}:hale-platform_wordpress-${{ github.sha }}

  ecrCleanDev:
    name: ECR clean - Dev
    runs-on: ubuntu-latest
    needs: [buildDev, deployDev]
    steps:
    - name: Run ECR clean-up script
      uses: ministryofjustice/ecr-images-cleaner-action@v1.0.1
      with:
        aws-access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_DEV }}
        aws-secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_DEV }}
        kube-cert: ${{ secrets.KUBE_CERT_DEV }}
        kube-token: ${{ secrets.KUBE_TOKEN_DEV }}
        kube-cluster: ${{ secrets.KUBE_CLUSTER_DEV }}
        kube-namespace: ${{ secrets.KUBE_NAMESPACE_DEV }}
        ecr-repo-name: ${{ secrets.ECR_NAME_DEV }}
        days-to-keep-old-images: 10
        max-old-images-to-keep: 24

  buildStaging:
    name: Build Staging and push to ECR
    if: github.event.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: Staging
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build NGINX image
        run: |
          docker build --pull --no-cache --tag hale-platform_nginx --file nginx.dockerfile .
      - name: Push NGINX image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_STAGING }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_STAGING }}
          region: eu-west-2
          local-image: hale-platform_nginx
          image: ${{ secrets.ECR_NAME_STAGING }}:hale-platform_nginx-${{ github.sha }}
      - name: Run Composer
        run: |
          composer config http-basic.composer.wp.dsd.io ${{ secrets.COMPOSER_USER }} ${{ secrets.COMPOSER_PASS }}
          composer install --prefer-dist --no-progress
      - name: Run NPM
        run: |
          npm install --prefix ./wordpress/wp-content/themes/hale
          npm run production --if-present --prefix ./wordpress/wp-content/themes/hale
      - name: Build WordPress image
        run: |
          docker build --pull --no-cache --tag hale-platform_wordpress --file wp.dockerfile .
      - name: Push WordPress image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_STAGING }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_STAGING }}
          region: eu-west-2
          local-image: hale-platform_wordpress:latest
          image: ${{ secrets.ECR_NAME_STAGING }}:hale-platform_wordpress-${{ github.sha }}

  deployStaging:
    name: Deploy to Staging
    if: github.event.ref == 'refs/heads/main'
    needs: buildStaging
    runs-on: ubuntu-latest
    environment: Staging
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Authenticate to the cluster
        env:
          KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER_STAGING }}
        run: |
          echo "${{ secrets.KUBE_CERT_STAGING }}" > ca.crt
          kubectl config set-cluster ${KUBE_CLUSTER} --certificate-authority=./ca.crt --server=https://${KUBE_CLUSTER}
          kubectl config set-credentials deploy-user --token=${{ secrets.KUBE_TOKEN_STAGING }}
          kubectl config set-context ${KUBE_CLUSTER} --cluster=${KUBE_CLUSTER} --user=deploy-user --namespace=${{ secrets.KUBE_NAMESPACE_STAGING }}
          kubectl config use-context ${KUBE_CLUSTER}
      - name: Apply Helm chart
        run: |
          helm upgrade wordpress helm_deploy/wordpress \
          --install \
          --namespace ${{ secrets.KUBE_NAMESPACE_STAGING }} \
          --values helm_deploy/wordpress/values.yaml \
          --set secrets.wpdbuser=${{ secrets.WORDPRESS_DB_USER_STAGING }} \
          --set secrets.wpdbhost=${{ secrets.WORDPRESS_DB_HOST_STAGING }} \
          --set secrets.wpdbname=${{ secrets.WORDPRESS_DB_NAME_STAGING }} \
          --set secrets.wpdbpassword=${{ secrets.WORDPRESS_DB_PASSWORD_STAGING }} \
          --set secrets.wpadminemail=${{ secrets.WORDPRESS_ADMIN_EMAIL_STAGING }} \
          --set secrets.wpadminuser=${{ secrets.WORDPRESS_ADMIN_USER_STAGING }} \
          --set secrets.wpadminpassword=${{ secrets.WORDPRESS_ADMIN_PASSWORD_STAGING }} \
          --set secrets.wpauthkey=${{ secrets.WORDPRESS_AUTH_KEY_FILE }} \
          --set secrets.wpauthsalt=${{ secrets.WORDPRESS_AUTH_SALT_FILE }} \
          --set secrets.wploggedinkey=${{ secrets.WORDPRESS_LOGGED_IN_KEY_FILE }} \
          --set secrets.wploggedinsalt=${{ secrets.WORDPRESS_LOGGED_IN_SALT_FILE }} \
          --set secrets.wpnoncekey=${{ secrets.WORDPRESS_NONCE_KEY_FILE }} \
          --set secrets.wpnoncesalt=${{ secrets.WORDPRESS_NONCE_SALT_FILE }} \
          --set secrets.wpsecureauthkey=${{ secrets.WORDPRESS_SECURE_AUTH_KEY_FILE }} \
          --set secrets.wpsecureauthsalt=${{ secrets.WORDPRESS_SECURE_AUTH_SALT_FILE }} \
          --set secrets.s3uploadsbucket=${{ secrets.S3_UPLOADS_BUCKET_STAGING }} \
          --set secrets.s3uploadsregion=${{ secrets.S3_UPLOADS_REGION_STAGING }} \
          --set secrets.s3uploadskey=${{ secrets.S3_UPLOADS_KEY_STAGING }} \
          --set secrets.s3uploadssecret=${{ secrets.S3_UPLOADS_SECRET_STAGING }} \
          --set configmap.envtype=${{ vars.ENV_TYPE_STAGING }} \
          --set configmap.servername=${{ secrets.KUBE_NAMESPACE_STAGING }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set ingress.metadata.annotations.setidentifier=hale-platform-ingress-${{ secrets.KUBE_NAMESPACE_STAGING }}-green \
          --set domain=${{ secrets.KUBE_NAMESPACE_STAGING }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set nginx.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_STAGING }}:hale-platform_nginx-${{ github.sha }} \
          --set wp.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_STAGING }}:hale-platform_wordpress-${{ github.sha }}

  ecrCleanStaging:
    name: ECR clean - Staging
    runs-on: ubuntu-latest
    needs: [buildStaging, deployStaging]
    steps:
    - name: Run ECR clean-up script
      uses: ministryofjustice/ecr-images-cleaner-action@v1.0.1
      with:
        aws-access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_STAGING }}
        aws-secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_STAGING }}
        kube-cert: ${{ secrets.KUBE_CERT_STAGING }}
        kube-token: ${{ secrets.KUBE_TOKEN_STAGING }}
        kube-cluster: ${{ secrets.KUBE_CLUSTER_STAGING }}
        kube-namespace: ${{ secrets.KUBE_NAMESPACE_STAGING }}
        ecr-repo-name: ${{ secrets.ECR_NAME_STAGING }}
        days-to-keep-old-images: 10
        max-old-images-to-keep: 24

  buildProd:
    name: Build Prod and push to ECR
    needs: deployStaging
    if: github.event.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build NGINX image
        run: |
          docker build --pull --no-cache --tag hale-platform_nginx --file nginx.dockerfile .
      - name: Push NGINX image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_PROD }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_PROD }}
          region: eu-west-2
          local-image: hale-platform_nginx
          image: ${{ secrets.ECR_NAME_PROD }}:hale-platform_nginx-${{ github.sha }}
      - name: Run Composer
        run: |
          composer config http-basic.composer.wp.dsd.io ${{ secrets.COMPOSER_USER }} ${{ secrets.COMPOSER_PASS }}
          composer install --prefer-dist --no-progress
      - name: Run NPM
        run: |
          npm install --prefix ./wordpress/wp-content/themes/hale
          npm run production --if-present --prefix ./wordpress/wp-content/themes/hale
      - name: Build WordPress image
        run: |
          docker build --pull --no-cache --tag hale-platform_wordpress --file wp.dockerfile .
      - name: Push WordPress image to ECR
        uses: jwalton/gh-ecr-push@v1
        with:
          access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_PROD }}
          secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_PROD }}
          region: eu-west-2
          local-image: hale-platform_wordpress:latest
          image: ${{ secrets.ECR_NAME_PROD }}:hale-platform_wordpress-${{ github.sha }}

  deployProd:
    name: Deploy to Production
    needs: buildProd
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Authenticate to the cluster
        env:
          KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER_PROD }}
        run: |
          echo "${{ secrets.KUBE_CERT_PROD }}" > ca.crt
          kubectl config set-cluster ${KUBE_CLUSTER} --certificate-authority=./ca.crt --server=https://${KUBE_CLUSTER}
          kubectl config set-credentials deploy-user --token=${{ secrets.KUBE_TOKEN_PROD }}
          kubectl config set-context ${KUBE_CLUSTER} --cluster=${KUBE_CLUSTER} --user=deploy-user --namespace=${{ secrets.KUBE_NAMESPACE_PROD }}
          kubectl config use-context ${KUBE_CLUSTER}
      - name: Apply Helm charts
        run: |
          helm upgrade wordpress helm_deploy/wordpress \
          --install \
          --namespace ${{ secrets.KUBE_NAMESPACE_PROD }} \
          --values helm_deploy/wordpress/values.yaml \
          --set secrets.wpdbuser=${{ secrets.WORDPRESS_DB_USER_PROD }} \
          --set secrets.wpdbhost=${{ secrets.WORDPRESS_DB_HOST_PROD }} \
          --set secrets.wpdbname=${{ secrets.WORDPRESS_DB_NAME_PROD }} \
          --set secrets.wpdbpassword=${{ secrets.WORDPRESS_DB_PASSWORD_PROD }} \
          --set secrets.wpadminemail=${{ secrets.WORDPRESS_ADMIN_EMAIL }} \
          --set secrets.wpadminuser=${{ secrets.WORDPRESS_ADMIN_USER }} \
          --set secrets.wpadminpassword=${{ secrets.WORDPRESS_ADMIN_PASSWORD }} \
          --set secrets.wpauthkey=${{ secrets.WORDPRESS_AUTH_KEY_FILE }} \
          --set secrets.wpauthsalt=${{ secrets.WORDPRESS_AUTH_SALT_FILE }} \
          --set secrets.wploggedinkey=${{ secrets.WORDPRESS_LOGGED_IN_KEY_FILE }} \
          --set secrets.wploggedinsalt=${{ secrets.WORDPRESS_LOGGED_IN_SALT_FILE }} \
          --set secrets.wpnoncekey=${{ secrets.WORDPRESS_NONCE_KEY_FILE }} \
          --set secrets.wpnoncesalt=${{ secrets.WORDPRESS_NONCE_SALT_FILE }} \
          --set secrets.wpsecureauthkey=${{ secrets.WORDPRESS_SECURE_AUTH_KEY_FILE }} \
          --set secrets.wpsecureauthsalt=${{ secrets.WORDPRESS_SECURE_AUTH_SALT_FILE }} \
          --set secrets.s3uploadsbucket=${{ secrets.S3_UPLOADS_BUCKET_PROD }} \
          --set secrets.s3uploadsregion=${{ secrets.S3_UPLOADS_REGION_PROD }} \
          --set secrets.s3uploadskey=${{ secrets.S3_UPLOADS_KEY_PROD }} \
          --set secrets.s3uploadssecret=${{ secrets.S3_UPLOADS_SECRET_PROD }} \
          --set configmap.servername=${{ secrets.KUBE_NAMESPACE_PROD }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set configmap.wpenvtype=${{ secrets.WP_ENV_TYPE }} \
          --set ingress.metadata.annotations.setidentifier=hale-platform-ingress-${{ secrets.KUBE_NAMESPACE_PROD }}-green \
          --set domain=${{ secrets.KUBE_NAMESPACE_PROD }}.apps.live.cloud-platform.service.justice.gov.uk \
          --set nginx.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_PROD }}:hale-platform_nginx-${{ github.sha }} \
          --set wp.image.repository=754256621582.dkr.ecr.eu-west-2.amazonaws.com/${{ secrets.ECR_NAME_PROD }}:hale-platform_wordpress-${{ github.sha }}

  ecrCleanProd:
    name: ECR clean - Prod
    runs-on: ubuntu-latest
    needs: [buildProd, deployProd]
    steps:
    - name: Run ECR clean-up script
      uses: ministryofjustice/ecr-images-cleaner-action@v1.0.1
      with:
        aws-access-key-id: ${{ secrets.ECR_AWS_ACCESS_KEY_ID_PROD }}
        aws-secret-access-key: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY_PROD }}
        kube-cert: ${{ secrets.KUBE_CERT_PROD }}
        kube-token: ${{ secrets.KUBE_TOKEN_PROD }}
        kube-cluster: ${{ secrets.KUBE_CLUSTER_PROD }}
        kube-namespace: ${{ secrets.KUBE_NAMESPACE_PROD }}
        ecr-repo-name: ${{ secrets.ECR_NAME_PROD }}
        days-to-keep-old-images: 10
        max-old-images-to-keep: 24
